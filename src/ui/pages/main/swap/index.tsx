import { t } from "i18next";
import s from "./styles.module.scss";

/** Placeholder: swap is not wired up yet. */
const Swap = () => {
  return (
    <div className={s.placeholder}>
      <h1>{t("nav.swap")}</h1>
    </div>
  );
};

export default Swap;
